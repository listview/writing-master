# 舒适阅读闸门（v2.2）

本文件把“读者轻松、顺畅、低负担地读下去”变成可执行的输入、写作和交付检查。这里的“舒适”是降低读者重建信息的负担，不是把黑暗、紧张、悲伤或复杂度削平。它不替代章节因果、连续性或题材模式规则；它负责在这些规则之间做取舍。默认警戒线可以按项目档放宽，但不能取消定位、因果、连续性、行动结果和具体章末变化。

## 1. 资料分层与最小加载包

先按层级取资料，不以“读得越多越稳”为目标。本文所说的“阅读单元”，通常指一个段落、一个完整的动作—结果，或一轮有来回的对话；高负担信息主要指本项目与最近一至三章中尚未出现的内容：

| 层级 | 默认内容 | 何时读取 |
| --- | --- | --- |
| L0 | 本次请求、输出格式、明确保留项和禁改项 | 每次必读 |
| L1 | 项目总档，优先 `00_项目上下文总档.md` 或等价的项目状态文件 | 处理具体小说时必读 |
| L2 | 当前章节/大纲与最近一至三章；先读上一章末的具体条件 | 续写、优化和连续性诊断必读 |
| L3 | 一份最贴近任务的参考：章节执行、重写诊断、长篇架构或选定模式 | 首轮只选相关的一份；必要时再加一份 |
| L4 | 连续性表、人物卡、规则表、卷纲、文风样稿等 | L0–L3 无法判断具体问题时定向补读 |
| L5 | 样本综合文档 | 用户明确要求分析来源/样本时才读；原小说默认不读，也不作为项目事实或声线来源 |

默认首轮上下文为“一份项目总档 + 当前章 + 最近一至三章 + 不超过两份任务参考”。目标章已经包含在最近章节中时不要重复加载。资料过大时，先提取时间、地点、伤势、资源、物件、知情边界和禁改事实，再放下无关原文；不要把整卷、十一部样本和所有分析文档一起塞进上下文。

需要选择题材模式时，读取所选模式的相关段落并替代一份任务参考，不把整份模式文档再叠加进首轮上下文。

如果项目没有名为 `00_项目上下文总档.md` 的文件，使用最接近的总纲、创作进度或连续性状态文件，不因为缺少固定文件名而全量读取目录。发现资料冲突时沿用：本次明确要求 > 已发布正文事实 > 项目档 > 章节任务卡 > 通用规则 > 样本机制。

## 2. 单发动机与信息预算

### 2.1 先写一行读者承诺

内部先补齐：

> 这一章读者主要想看谁，用什么办法，在什么压力下，得到哪种可见变化？

据此只选一个主叙事发动机（升级、求生经营、群像、志怪、规则冒险、谍战或制度修仙），最多加一个副模式。副模式删掉后若主问题仍成立，就把它降为一两个服务主线的局部技巧；不要在同一场戏平均调用多种模式。

### 2.2 高负担信息的默认警戒线

以下内容会占用读者记忆：新专名、新规则、关键数字/期限、时空跳转、多步骤操作、复杂阵营关系和大段回忆解释。默认每个阅读单元只引入一至两个高负担单元；同场真正必要的新专名默认不超过三个。超过警戒线时，先用职能、关系或可见效果落地，再延后正式名称、分类和历史。

高负担信息出现后，下一阅读单元应回到人物的判断、选择、动作、可见反应或结果。连续两个阅读单元都只解释设定时，合并、改成冲突中的短句，或把信息推迟到使用前。

这些是报警线，不是“每章只能有多少字/名词”的硬配额。复杂场景可以暂时超过，但必须让每个新增信息立即服务当前选择，并给出反馈或代价。

## 3. 技术细节与操作记录过滤

账册、药效、伤势、证物、验力线、阵纹、手续和数据都可以是戏，但必须通过至少一个功能测试：

1. 它是否改变人物现在的选择、风险或行动顺序？
2. 读者是否必须立刻知道精确数字、步骤或名称，才能理解这次冲突？
3. 它是否会在本场或下一场产生可见结果、证据、关系变化或代价？

三问都是否定时，删除或后移。只有一问为“是”时，保留最低必要信息；两问以上为“是”时，也只代表它有资格进入正文，不代表必须完整展开。若同场有多项细节都通过测试，只详写最能暴露人物、制造意外或改变结果的一项，其余合并为一句结果。

再做一次省略测试：读者若不知道具体步骤，是否仍能相信人物做成了这件事并理解其代价？若能，直接跳到关键接触点或结果；只有步骤本身可能失败、暴露秘密、引发争执或构成本场乐趣时才展开。

### 3.1 操作记录的压缩方式

- 连续检查、计算或翻找，合并为“意图 → 关键接触点 → 结果/代价”；除非某一步失败、暴露或改变证据链，不逐帧写手指、纸页、刻度和呼吸。
- 连续三句流程/技术说明后，必须出现人物判断、利益冲突、动作、风险或结果；如果三句本身各自都改变了证据、选择或结果，可视为同一段有效行动，不必机械插入对白。否则回到场景主问题重写。
- 同一伤势只保留一个主限制；疼痛、麻木、眩晕等重复描写若不改变选择，删掉或合并。
- 规则说明优先改写成价格、手续、试验、误判、失败或谁承担责任；不要让角色轮流朗读说明书。
- 动作已经表达情绪或事实时，禁止再用“他意识到/这意味着/显然/不难看出”等旁白翻译一次。
- 同一种操作、招式或判断第一次已经验证后，后续默认压缩到结果；只有新变量改变了胜负条件时再展开。
- 准备、救援、后勤和制度场景不要因为“每一步都合理”就逐项展示。让读者记住一项关键选择、一项可见阻力和一个结果即可，其他正确步骤留在作者记录里。

## 4. 场景节奏与即时回报

每场围绕一个局部问题运行：

> 目标 → 阻碍 → 判断/选择 → 行动 → 结果 → 代价或新条件

没有结果、没有改变位置/物件/关系/认知/资源/时间、也没有新选择的场景，合并或删除。这里的“行动”也包括签字、拒绝、发问、公开、交换、让位、承诺、改名册或要求留痕等社会行动，不要求每场都靠打斗或大幅位移推进。主要行动尽量在同场得到即时反馈；如果必须延迟结算，就先给一个可见的部分结果和下一步行动，不让连续多场只停留在准备、检查或等待。

高压段落连续五个阅读单元（或约四百至六百字，取较短者）仍没有选择、结果、关系变化或条件变化时，视为节奏报警。纯氛围最多连续两个阅读单元，随后回到人物正在做的事。数字只是帮助发现拖滞，不替代对正文的判断。

每章至少完成一个小兑现，并让代价真实存在：资源减少、伤势加重、身份改变、暴露增加、关系承诺、证据换手或选择空间收窄均可。回报不必是战力数字，但必须能被读者指出。

## 5. 人物声线与情绪负担

主要人物的台词由身份、知识来源、利益焦点和压力反应共同决定。写完后遮住姓名复读：若人物仍可互相替换，改写台词的目标、信息边界或回答方式，而不是给每人添加口头禅。用户明确要求多人都发言时，可以让每个指定人物说一句或几句，但每句都要改变利益、信息、证据、选择或关系；不要用“平均轮流一句”代替功能性对白。

情绪通常从外部动作/物件、身体感受、直接念头三层中取两层。情绪只有在改变选择、站位、语气、关系成本或风险判断时才展开；不让每个动作都带一组“神情＋心理解释”。

## 6. Comfort Gate（交付前快速闸门）

以下八项在内部逐项回答“是/否”：

1. 开头几个阅读单元已让读者知道谁、在哪里（或可识别锚点）以及眼前异动/任务。
2. 本章只有一个可用一句话说清的主问题和一个主发动机。
3. 每个主要场景都有结果或具体条件变化。
4. 新专名、规则和技术细节没有超过当前选择所需的负担。
5. 常规或重复过程已经省略，读者不需要模拟完整操作才能继续阅读；连续微动作与翻译性旁白已删除。
6. 技术/设定说明后出现了人物行动、冲突、反应或验证结果。
7. 主要人物去掉姓名后仍有可辨声线，且本章有即时小兑现与真实代价。
8. 章末改变了物件、期限、名单、身份、资源、规则、关系或伤势中的至少一项。

以下任一项命中即为硬失败，先回到结构修改，不用润色或总分掩盖：开头没有定位；主问题/主模式不唯一；连续性冲突；核心规则未验证却决定胜负；主要行动没有结果；章末只有“危险将至/有人注视/意味深长一笑”；连续三句技术说明、三个无效微动作或重复解释仍未修复（除非这些句子/动作各自改变了证据、选择或结果）。

内部快速盲读只需回答四句话：读者能否在三十秒内说出“谁在何处、要什么、被什么阻碍、付出什么代价/得到什么结果”？若不能，优先删、并、移信息，而不是继续加解释。

## 7. 七种模式的局部适配

选定模式后，只应用对应一行的节奏提醒；不要把七行拼成新的混合风格。模式可以改变信息密度和回报形态，但不能豁免 Comfort Gate 的硬失败项。

| 主模式 | v2.2 适配提醒 |
| --- | --- |
| 快节奏升级 | 一章聚焦一项核心新能力或胜负变化；首次使用尽快以试用、部分结果或代价验证。允许一个连带表现/副作用，但围观反应不能代替结果。 |
| 凡人生存经营 | 只保留会改变本次取舍的价格、期限和账目；说明后立刻结算资源、关系或待遇。 |
| 家族群像 | 视角切换先落地位置、代表利益和已知信息；议事围绕一项选择，不让配角平均轮流发言。 |
| 民俗志怪 | 先给一个日常锚点，再让一个异常侵入；方言和民俗细节必须改变行动、判断或代价。 |
| 规则诡异冒险 | 明确事实、猜测和已验证规则；每次试错带来选择或代价，队员承担可被打断的不同功能。 |
| 朝堂谍战悬疑 | 关键线索绑定经手人、权限和风险；制度细节服务责任与利益，不写成公文说明书。 |
| 现代制度修仙 | 一场突出一个核心制度或指标；数字只在改变资格、价格、时间或风险时出现。 |
