# 贡献指南

欢迎提交问题、建议和改进。

## 提交前检查

- 保持 `SKILL.md` 的入口职责清晰，详细模式放在 `references/`。
- 新增规则应说明它改变了什么判断，避免加入泛泛的写作口号。
- 不添加特定作者的可识别仿写要求，不把单部作品的设定写成通用规则。
- 参考文件之间使用相对路径，且链接目标真实存在。
- 修改后运行 `quick_validate.py`，再用一个真实但无版权风险的示例请求检查行为。

## 提交内容

Issue 请包含：使用场景、输入资料类型、实际输出、期望输出和可复现步骤。Pull request 请说明修改原因及是否改变默认路由。
